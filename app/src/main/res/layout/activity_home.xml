<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".Home.HomeActivity"
    android:id="@+id/homeActivity">

    <TextView
        android:id="@+id/id_context_label"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:onClick="changeContext"
        android:text="Hello"
        android:textColor="@android:color/white"
        android:textSize="30sp"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="70dp"/>

    <ImageView
        android:id="@+id/config_btn"
        android:layout_width="50dp"
        android:layout_height="50dp"
        android:layout_gravity="center_vertical"
        android:contentDescription="Settings Icon"
        android:onClick="showSettings"
        android:src="@drawable/ic_settings_black_24dp"
        android:tint="@color/secondaryColor"
        android:layout_alignParentEnd="true"
        android:layout_marginEnd="20dp"
        android:layout_marginTop="50dp"/>

    <Button
        android:id="@+id/id_all_apps_button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:background="@drawable/ic_allapps"
        android:clickable="true"
        android:onClick="showAllAppsMenu"
        android:focusable="true" />


    <LinearLayout
        android:id="@+id/id_context_apps_list"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_above="@+id/id_all_apps_button"
        android:orientation="horizontal">
    </LinearLayout>



</RelativeLayout>